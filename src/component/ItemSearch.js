import React from 'react'

const ItemSearch = ({productName,price}) => {

    //const {productName,price} =props;
    //console.log("Props data :: ",props);
    return <h1 className="error">Prodcut name : {productName} and price is {price}</h1>
}

export default ItemSearch;